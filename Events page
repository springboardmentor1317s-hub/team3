<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CampusEventHub – Events</title>

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">

<style>
*{
    margin:0; padding:0;
    box-sizing:border-box;
    font-family:'Poppins',sans-serif;
}

body{
    background:#F8FAFC;
    overflow-x:hidden;
    color:#1A1A1A;
}

/* --- PASTEL ORBS --- */
.bg-orbs{
    position:fixed;
    top:0; left:0;
    width:100%; height:100%;
    pointer-events:none;
    overflow:hidden;
    z-index:-1;
}

.orb{
    position:absolute;
    width:320px;
    height:320px;
    border-radius:50%;
    filter:blur(70px);
    opacity:0.55;
    animation:orbFloat 12s ease-in-out infinite;
}

.orb:nth-child(1){
    background:radial-gradient(circle,#FFB7D9,#FF4EB8);
    top:5%; left:10%;
}
.orb:nth-child(2){
    background:radial-gradient(circle,#A8FFCE,#60D394);
    top:60%; left:65%;
    animation-delay:2s;
}
.orb:nth-child(3){
    background:radial-gradient(circle,#FFB7D9,#FF4EB8);
    top:25%; left:80%;
    animation-delay:4s;
}

@keyframes orbFloat{
    0%,100%{ transform:translate(0,0) scale(1); }
    50%{ transform:translate(60px,-60px) scale(1.2); }
}

/* --- NAVBAR --- */
nav{
    width:100%;
    padding:25px 80px;
    display:flex;
    justify-content:space-between;
    align-items:center;
    position:fixed;
    top:0;
    background:rgba(255,255,255,0.7);
    backdrop-filter:blur(10px);
    box-shadow:0 4px 20px rgba(0,0,0,0.08);
    z-index:10;
}

nav .logo{
    font-size:30px;
    font-weight:800;
    color:#FF4EB8;
}

nav ul{
    display:flex;
    gap:40px;
}

nav ul li{ list-style:none; }

nav ul li a{
    text-decoration:none;
    color:#1A1A1A;
    font-size:16px;
    position:relative;
    font-weight:500;
}

nav ul li a:hover{
    color:#FF4EB8;
}

nav ul li a.active{
    color:#FF4EB8;
}

nav ul li a::after{
    content:"";
    width:0%;
    height:2px;
    background:#FF4EB8;
    position:absolute;
    left:0; bottom:-5px;
    transition:0.4s;
}

nav ul li a:hover::after{
    width:100%;
}

/* --- PAGE HEADER --- */
.page-header{
    text-align:center;
    margin-top:150px;
    padding:20px;
}

.page-header h1{
    font-size:60px;
    font-weight:800;
    color:#1A1A1A;
}

.page-header p{
    font-size:20px;
    color:#4b4b4b;
    margin-top:10px;
}

/* --- FLOAT ANIMATION FOR CARDS --- */
@keyframes floatCard{
    0%{ transform:translateY(0); }
    50%{ transform:translateY(-20px); }
    100%{ transform:translateY(0); }
}

/* --- EVENTS GRID --- */
.event-grid{
    display:grid;
    grid-template-columns:repeat(auto-fit, minmax(300px, 1fr));
    gap:40px;
    padding:60px 80px 120px 80px;
}

.event-card{
    background:#FFF6E9;
    border-radius:25px;
    padding:25px;
    color:#1A1A1A;
    box-shadow:0 8px 25px rgba(0,0,0,0.08);
    transition:0.3s;
    animation:floatCard 6s ease-in-out infinite;
}

.event-card:nth-child(2){ animation-delay:1s; }
.event-card:nth-child(3){ animation-delay:2s; }
.event-card:nth-child(4){ animation-delay:0.5s; }

.event-card:hover{
    transform:translateY(-15px);
    box-shadow:0 12px 35px rgba(0,0,0,0.15);
}

.event-card h3{
    font-size:24px;
    margin-bottom:12px;
    color:#FF4EB8;
}

.event-card p{
    font-size:15px;
    color:#4b4b4b;
}

.event-card a{
    display:inline-block;
    margin-top:15px;
    text-decoration:none;
    font-weight:600;
    color:#60D394;
    transition:0.3s;
}

.event-card a:hover{
    color:#FF4EB8;
}
</style>
</head>

<body>

<!-- Pastel Orbs -->
<div class="bg-orbs">
    <div class="orb"></div>
    <div class="orb"></div>
    <div class="orb"></div>
</div>

<!-- Navbar -->
<nav>
    <div class="logo">CampusEventHub</div>
    <ul>
        <li><a href="index.html">Home</a></li>
        <li><a class="active">Events</a></li>
        <li><a href="register.html">Register</a></li>
        <li><a href="login.html">Login</a></li>
    </ul>
</nav>

<!-- Header -->
<section class="page-header">
    <h1>Upcoming Events</h1>
    <p>Get a glimpse of all the exciting inter-college events happening around you.</p>
</section>

<!-- Event Grid -->
<div class="event-grid">

    <div class="event-card">
        <h3>Hackathon X</h3>
        <p>Build crazy projects in 24 hours. Open to all innovators.</p>
        <a href="#">View Details →</a>
    </div>

    <div class="event-card">
        <h3>Sports Meet 2025</h3>
        <p>Cricket, Football, Athletics & more. Compete & shine!</p>
        <a href="#">View Details →</a>
    </div>

    <div class="event-card">
        <h3>National Cultural Fest</h3>
        <p>Dance, Drama, Music & Art — Celebrate creativity.</p>
        <a href="#">View Details →</a>
    </div>

    <div class="event-card">
        <h3>Debate Championship</h3>
        <p>Showcase your thinking, confidence & communication.</p>
        <a href="#">View Details →</a>
    </div>

    <div class="event-card">
        <h3>AI & ML Workshop</h3>
        <p>Hands-on Python, ML models & real-life projects.</p>
        <a href="#">View Details →</a>
    </div>

    <div class="event-card">
        <h3>Startup Pitch Battle</h3>
        <p>Present ideas, impress mentors & win funding.</p>
        <a href="#">View Details →</a>
    </div>

</div>

</body>
</html>
