<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CampusEventHub</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">

<style>
*{ margin:0; padding:0; box-sizing:border-box; font-family:'Poppins',sans-serif; }

body{
    background:#F8FAFC;
    color:#1A1A1A;
    overflow-x:hidden;
    transition:all 0.4s ease;
}

/* Dark mode styles */
body.dark-mode{
    background:#121212;
    color:#f1f1f1;
}
body.dark-mode nav{
    background:rgba(20,20,20,0.7);
    box-shadow:0 4px 20px rgba(0,0,0,0.5);
}
body.dark-mode nav .logo{ color:#FF79B0; }
body.dark-mode nav ul li a{ color:#f1f1f1; }
body.dark-mode nav ul li a:hover{ color:#FF79B0; }
body.dark-mode .event-card{ background:#1E1E1E; color:#f1f1f1; box-shadow:0 6px 20px rgba(0,0,0,0.5);}
body.dark-mode .event-card h3{ color:#FF79B0; }
body.dark-mode .event-card a{ color:#60D394; }
body.dark-mode .hero p{ color:#ccc; }
body.dark-mode .hero h1{ color:#FF79B0; }

/* --- PASTEL ORB BACKGROUND --- */
.bg-orbs{ position:fixed; top:0; left:0; width:100%; height:100%; pointer-events:none; overflow:hidden; z-index:-1; }
.orb{ position:absolute; width:320px; height:320px; border-radius:50%; filter:blur(70px); opacity:0.5; animation:orbFloat 12s ease-in-out infinite; }
.orb:nth-child(1){ background:radial-gradient(circle,#FFB7D9,#FF4EB8); top:5%; left:5%; animation-delay:0s; }
.orb:nth-child(2){ background:radial-gradient(circle,#A8FFCE,#60D394); top:50%; left:65%; animation-delay:3s; }
.orb:nth-child(3){ background:radial-gradient(circle,#FFB7D9,#FF4EB8); top:20%; left:80%; animation-delay:6s; }

@keyframes orbFloat{ 0%,100%{ transform:translate(0,0) scale(1);} 50%{ transform:translate(60px,-60px) scale(1.2);} }

/* --- NAVBAR --- */
nav{
    width:100%;
    padding:25px 80px;
    display:flex;
    justify-content:space-between;
    align-items:center;
    position:fixed;
    top:0;
    background:rgba(255,255,255,0.7);
    backdrop-filter:blur(10px);
    box-shadow:0 4px 20px rgba(0,0,0,0.08);
    z-index:10;
}
nav .logo{ font-size:30px; font-weight:800; color:#FF4EB8; }
nav ul{ display:flex; gap:40px; align-items:center; }
nav ul li{ list-style:none; }
nav ul li a{ text-decoration:none; color:#1A1A1A; font-size:16px; font-weight:500; position:relative; }
nav ul li a:hover{ color:#FF4EB8; }
nav ul li a::after{ content:""; width:0%; height:2px; background:#FF4EB8; position:absolute; left:0; bottom:-5px; transition:0.4s; }
nav ul li a:hover::after{ width:100%; }

/* --- DARK MODE TOGGLE --- */
.toggle-container{
    display:flex;
    align-items:center;
    gap:8px;
    margin-left:20px;
}

.toggle-icon{
    font-size:18px;
    user-select:none;
    transition:0.3s;
}

.toggle-btn{
    cursor:pointer;
    width:50px; height:25px;
    border-radius:20px;
    background:#ccc;
    position:relative;
    flex-shrink:0;
}
.toggle-btn::after{
    content:'';
    position:absolute;
    width:21px; height:21px;
    background:white;
    border-radius:50%;
    top:2px; left:2px;
    transition:0.3s;
}
.toggle-btn.active{ background:#FF4EB8; }
.toggle-btn.active::after{ left:27px; }

/* --- HERO --- */
.hero{ text-align:center; margin-top:180px; padding:40px; }
.hero h1{ font-size:65px; font-weight:800; line-height:80px; color:#1A1A1A; transition: color 0.4s; }
.hero p{ font-size:22px; margin-top:15px; color:#5b5b5b; transition: color 0.4s; }

/* --- BUTTON --- */
.btn{ display:inline-block; margin-top:35px; padding:15px 50px; background:#FF4EB8; color:white; font-size:18px; border-radius:40px; border:none; box-shadow:0 4px 15px rgba(255,78,184,0.4); cursor:pointer; transition:0.3s; }
.btn:hover{ transform:scale(1.08); box-shadow:0 5px 25px rgba(255,78,184,0.55); }

/* --- EVENT CARDS --- */
.event-section{ margin-top:80px; display:flex; justify-content:center; gap:55px; padding:0 40px 120px; flex-wrap:wrap; }
.event-card{ width:300px; background:#FFF6E9; border-radius:25px; padding:25px; color:#1A1A1A; box-shadow:0 6px 20px rgba(0,0,0,0.08); transition: all 0.4s; }
.event-card h3{ font-size:24px; margin-bottom:10px; color:#FF4EB8; transition: color 0.4s; }
.event-card p{ font-size:15px; color:#4b4b4b; transition: color 0.4s; }
.event-card a{ display:inline-block; margin-top:15px; color:#60D394; text-decoration:none; font-weight:600; transition: color 0.4s; }
.event-card a:hover{ color:#FF4EB8; }
</style>
</head>
<body>

<!-- Pastel Orbs -->
<div class="bg-orbs">
    <div class="orb"></div>
    <div class="orb"></div>
    <div class="orb"></div>
</div>

<!-- Navbar -->
<nav>
    <div class="logo">CampusEventHub</div>
    <ul>
        <li><a href="index.html" style="color:#FF4EB8;">Home</a></li>
        <li><a href="events.html">Events</a></li>
        <li><a href="register.html">Register</a></li>
        <li><a href="login.html">Login</a></li>
        <li>
            <div class="toggle-container">
                <span class="toggle-icon" id="toggleIcon">☀️</span>
                <div class="toggle-btn" id="darkToggle"></div>
            </div>
        </li>
    </ul>
</nav>

<!-- Hero -->
<section class="hero">
    <h1>The Future of Inter-College<br>Event Discovery</h1>
    <p>Explore hackathons, cultural fests, sports meets & more — all in one place.</p>
    <a href="events.html" class="btn">Explore Events</a>
</section>

<!-- Event Cards -->
<div class="event-section">
    <div class="event-card">
        <h3>Hackathon X</h3>
        <p>24 Hours. Infinite Ideas. Compete with the sharpest minds.</p>
        <a href="#">Learn More →</a>
    </div>
    <div class="event-card">
        <h3>Sports Meet</h3>
        <p>Unleash your spirit — Cricket, Football, Athletics & more.</p>
        <a href="#">Join Now →</a>
    </div>
    <div class="event-card">
        <h3>Cultural Fest</h3>
        <p>Dance • Drama • Music • Art — A celebration of creativity.</p>
        <a href="#">Explore →</a>
    </div>
</div>

<script>
const toggleBtn = document.getElementById('darkToggle');
const toggleIcon = document.getElementById('toggleIcon');
const body = document.body;

// Load preference
if(localStorage.getItem('darkMode') === 'enabled'){
    body.classList.add('dark-mode');
    toggleBtn.classList.add('active');
    toggleIcon.textContent = '🌙';
}

// Toggle
toggleBtn.addEventListener('click',()=>{
    body.classList.toggle('dark-mode');
    toggleBtn.classList.toggle('active');
    if(body.classList.contains('dark-mode')){
        localStorage.setItem('darkMode','enabled');
        toggleIcon.textContent = '🌙'; // moon for dark
    } else{
        localStorage.setItem('darkMode','disabled');
        toggleIcon.textContent = '☀️'; // sun for light
    }
});
</script>

</body>
</html>
